---
title: Sprint4. Del modelo a la aplicación
description: En esta actividad, el alumnado se enfrentará al reto de construir una aplicación real de Machine Learning en Python para resolver un problema de clasificación de imágenes “Perro/Gato”. Para ello deberá utilizar la librería TensorFlow/Keras. El proyecto abarcará el ciclo de vida completo del sw. de IA: recolección y preparación del datasheet, definición de la arquitectura del modelo, entrenamiento y evaluación crítica de su rendimiento.
---




<!--

Diagrama de Fases de Inferencia (Usar un Modelo ya entrenado)

Este es el diagrama que describe exactamente el snippet de código que te da Teachable Machine.

    Fase 1: Cargar Recursos

        Cargar el Modelo (el archivo .h5 o la carpeta SavedModel).

        Cargar las Etiquetas (el archivo labels.txt).

    Fase 2: Adquirir Datos Nuevos

        Cargar una imagen (desde un archivo, la webcam, el móvil...).

    Fase 3: Pre-procesamiento

        La imagen DEBE transformarse para ser idéntica a las de entrenamiento.

        Esto incluye:

            Cambiar tamaño (ej. 224x224 píxeles).

            Recortar (si es necesario).

            Normalizar los valores de los píxeles (ej. de [0, 255] a [-1, 1]).

    Fase 4: Predicción

        Se usa el método .predict().

        El modelo recibe la imagen pre-procesada.

        El modelo devuelve un array de números (ej. [0.98, 0.02]).

    Fase 5: Post-procesamiento

        El código "humano" interpreta ese array.

        Se busca el índice del número más alto (en [0.98, 0.02] sería el índice 0).

        Se usa ese índice para buscar en la lista de etiquetas (labels.txt).

        Resultado: Se muestra la etiqueta correspondiente (ej. "Perro").

-->